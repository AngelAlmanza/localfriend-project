export interface CreateServiceCategoryDTO {
  name: string;
  description: string;
  imageUrl: string;
}

export interface UpdateServiceCategoryDTO {
  id: string;
  name: string;
  description: string;
  imageUrl: string;
}
