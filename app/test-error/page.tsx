// Ruta temporal para probar la página de error (app/error.tsx).
// Eliminar cuando ya no sea necesaria.
export default function TestErrorPage(): never {
  throw new Error("Error de prueba — esta ruta existe temporalmente para probar la página de error.");
}
